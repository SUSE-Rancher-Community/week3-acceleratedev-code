apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: pytest
spec:
  resources:
    inputs:
      - name: source
        type: git
  steps:
    - name: run-pytest
      image: python:3
      workingDir: /workspace/source
      command: ["pytest"]
      args: ["."]